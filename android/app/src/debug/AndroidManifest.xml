<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--    Starting with Android 13 (API 33), Google replaced READ_EXTERNAL_STORAGE and WRITE_EXTERNAL_STORAGE with media-specific permissions-->

    <!-- Old storage permissions (for backward compatibility Android 12 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- New audio-specific permission for Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!--    For Android 11+, you’ll store in the app’s directory.-->

</manifest>
