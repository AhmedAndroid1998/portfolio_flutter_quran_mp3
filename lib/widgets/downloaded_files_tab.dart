import 'package:flutter/material.dart';

class DownloadedFilesTab extends StatelessWidget {
  const DownloadedFilesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
