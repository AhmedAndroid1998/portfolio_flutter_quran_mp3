import 'package:flutter/material.dart';

class DownloadingFilesTab extends StatelessWidget {
  const DownloadingFilesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
